#!/usr/bin/env bash
set -eu
[ "${BASH_VERSINFO[0]}" -ge 3 ] && set -o pipefail

cat <<-EOM
Manage golangci-lint with asdf.

- https://github.com/bry-guy/asdf-golangci-lint
- https://golangci-lint.run
EOM

